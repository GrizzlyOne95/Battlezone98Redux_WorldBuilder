name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release (e.g. v1.0.4)'
        required: true

permissions:
  contents: write

env:
  RELEASE_TAG: ${{ github.event_name == 'workflow_dispatch' && inputs.version || github.ref_name }}

jobs:
  build:
    name: Build ${{ matrix.platform }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux
            os: ubuntu-latest
            asset: world-builder-linux
          - platform: windows
            os: windows-latest
            asset: world-builder-windows.exe
          - platform: macos
            os: macos-latest
            asset: world-builder-macos.zip

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Linux system dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y python3-tk python3-dev libjpeg-dev libpng-dev

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt

      - name: Verify imports and syntax
        run: |
          python -m py_compile world_builder.py legacy_port_cli.py legacy_preflight.py legacy_preflight_hook.py legacy_batch.py legacy_batch_gui.py
          python -c "import world_builder; import legacy_port_cli; import legacy_preflight; import legacy_batch; import legacy_batch_gui; print('Import successful')"

      - name: Build Linux executable
        if: runner.os == 'Linux'
        run: |
          pyinstaller --noconfirm --clean --onefile --copy-metadata imageio --name world-builder-linux --add-data="BZONE.ttf:." world_builder.py
          test "$(stat -c%s dist/world-builder-linux)" -gt 1000000
          pyi-archive_viewer dist/world-builder-linux -l | grep -E 'imageio.*\.dist-info'

      - name: Build Windows executable
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          pyinstaller --noconfirm --clean --onefile --windowed --copy-metadata imageio --icon=wb.ico --name world-builder-windows --add-data="BZONE.ttf;." world_builder.py
          $asset = Get-Item .\dist\world-builder-windows.exe
          if ($asset.Length -lt 1000000) { throw "Windows release asset is unexpectedly small: $($asset.Length) bytes" }
          $archive = pyi-archive_viewer .\dist\world-builder-windows.exe -l | Out-String
          if ($archive -notmatch 'imageio.*\.dist-info') { throw "Frozen Windows executable is missing imageio distribution metadata" }

      - name: Build Windows CLI executable
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          pyinstaller --noconfirm --clean --onefile --console --copy-metadata imageio --icon=wb.ico --name world-builder-cli --add-data="BZONE.ttf;." legacy_port_cli.py
          $asset = Get-Item .\dist\world-builder-cli.exe
          if ($asset.Length -lt 1000000) { throw "Windows CLI release asset is unexpectedly small: $($asset.Length) bytes" }
          $archive = pyi-archive_viewer .\dist\world-builder-cli.exe -l | Out-String
          if ($archive -notmatch 'imageio.*\.dist-info') { throw "Frozen Windows CLI executable is missing imageio distribution metadata" }

      - name: Build and package macOS app
        if: runner.os == 'macOS'
        run: |
          pyinstaller --noconfirm --clean --onedir --windowed --copy-metadata imageio --name world_builder --add-data="BZONE.ttf:." world_builder.py
          test -d dist/world_builder.app
          find dist/world_builder.app -path '*imageio*.dist-info/METADATA' -print -quit | grep .
          ditto -c -k --sequesterRsrc --keepParent dist/world_builder.app dist/world-builder-macos.zip
          test "$(stat -f%z dist/world-builder-macos.zip)" -gt 1000000

      - name: Upload packaged asset
        uses: actions/upload-artifact@v4
        with:
          name: world-builder-${{ matrix.platform }}
          path: dist/${{ matrix.asset }}
          if-no-files-found: error
          retention-days: 7

      - name: Upload Windows CLI asset
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: world-builder-cli
          path: dist/world-builder-cli.exe
          if-no-files-found: error
          retention-days: 7

  release:
    name: Publish release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download packaged assets
        uses: actions/download-artifact@v4
        with:
          pattern: world-builder-*
          path: release-assets
          merge-multiple: true

      - name: Verify release payload
        run: |
          ls -lh release-assets
          test "$(stat -c%s release-assets/world-builder-linux)" -gt 1000000
          test "$(stat -c%s release-assets/world-builder-windows.exe)" -gt 1000000
          test "$(stat -c%s release-assets/world-builder-cli.exe)" -gt 1000000
          test "$(stat -c%s release-assets/world-builder-macos.zip)" -gt 1000000

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          target_commitish: ${{ github.sha }}
          name: Release ${{ env.RELEASE_TAG }}
          body_path: docs/RELEASE_NOTES_${{ env.RELEASE_TAG }}.md
          files: release-assets/*
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
