# Application icon source

`branding/repo_icon.svg` is the canonical source artwork for the packaged WorldBuilder application icon. The Windows executable uses the generated `wb.ico` derived from this artwork.
